# Changelog

## 0.0.1 — 2026-08-29

- Scaffold mono-repo: `mobile/`, `backend/`, `dashboard/`, `ml/`, `docs/`, CI stubs.
- Five pipeline agents as labeled mocks; tunnel GNSS-loss demo on Home/Map.
- FastAPI in-memory `POST /trips/{trip_id}/points` (201/401).
- React dashboard Trip Replay with hardcoded mock GPS vs fused path.
- Add `docs/schema.sql`, `.env.example` files only (no secrets).
